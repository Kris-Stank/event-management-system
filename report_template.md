# Command-line Application Report — Staff CRUD (PostgreSQL)

**Student:** <Your Name>  
**Course / Group:** <Course Name / Group>  
**Date:** <YYYY-MM-DD>

## Objective
Create a Python CLI application that connects to PostgreSQL and performs full CRUD on the `staff` table, plus a complex SELECT with multiple predicates (AND/OR). Provide evidence screenshots for each operation and demonstrate SQL injection safety.

## Steps Performed (Evidence)
(Insert screenshots with these captions)
- **Create: list before**
- **Create: command**
- **Create: list after**
- **Read: list (all)**
- **Read: get <id>**
- **Update: record before**
- **Update: command**
- **Update: record after**
- **Delete: list/record before**
- **Delete: command**
- **Delete: list after**
- **Complex SELECT: SQL command**
- **Complex SELECT: result**
- **SQL injection: attempted payload**
- **SQL injection: safe outcome (no destructive effect)**
